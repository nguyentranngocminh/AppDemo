// import RootNavigation from '../navigations/RootNavigation';

// export default (state, action) => {
//   const newState = RootNavigation.router.getSateForAction(action, state);
//   return newState || state;
// };
